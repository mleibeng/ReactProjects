export default function Main() {
    return (
        <main>
            <form className="add-ingredients-form">
                <input type="text" aria-label="Add ingredient" placeholder="e.g. pepper"/>
                <button type="text">Add ingredient</button>
            </form>
        </main>
    )
}